import { SalesRep, ProductType, CustomerSource, ReportType } from './types';

export const REPORT_TYPES = Object.values(ReportType);
export const SALES_REPS = Object.values(SalesRep);
export const PRODUCT_TYPES = Object.values(ProductType);
export const CUSTOMER_SOURCES = Object.values(CustomerSource);

export const APP_TITLE = "法華山瑤池陵宮";
export const FORM_TITLE = "每日成交回報單";
